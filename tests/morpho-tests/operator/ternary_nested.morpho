// Nested ternary operators

var a = 3

for (var i = 0; i < 1; i+=1) {
    print (i<a ? "less" : (i==a ? "same" : "more"))
}

// expect: less
// expect: less
// expect: same
// expect: more
// expect: more
